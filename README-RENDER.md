# Render deployment notes

Environment variables:
- DB_NAME=defaultdb
- DB_USER=avnadmin
- DB_PASSWORD=<Aiven password>
- DB_HOST=<Aiven hostname>:<Aiven port>
- RENDER=true

Import the WordPress database SQL into the Aiven MySQL database before opening the site if this project has existing content.
